from django.contrib import admin

# Register your models here.

# accounts/admin.py (You can keep this empty or add custom admin for User if needed)
# The default User model is already registered by Django's admin app.